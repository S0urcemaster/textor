# Repository Guidelines

The specification in `_spec/index.md` is deprecated and kept as historical
working material only.

Do not treat the old spec as current project truth. Future specification work
should wait for the canon in `/home/sntr/code/agenttheory/.theo/index.xml` to be
further advanced, then rewrite the project specification on that canon basis.

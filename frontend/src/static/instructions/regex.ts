import { Instruction } from "../../app/model"

export const instruction_regex: Instruction = {
   name: 'regex',
   update: async (text: string) => {
      return 'not implemented'
   },
   manual: false
}